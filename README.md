just a POC that graphql queries are basically possible with serenity rest screenplay, though I would strongly 
recommend further libraries and model classes to better create the POST body

run with
     
     mvn verify